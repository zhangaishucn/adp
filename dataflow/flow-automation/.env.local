# Local development overrides for .env
# This file overrides Kubernetes service addresses with localhost for local development

# Override Kubernetes services with localhost
DEPLOYSERVICE_HOST=localhost
DEPLOYSERVICE_PORT=9703

USERMANAGEMENT_PRIVATE_HOST=localhost
USERMANAGEMENT_PRIVATE_PORT=30980

AUTHENTICATION_PRIVATE_HOST=localhost
AUTHENTICATION_PRIVATE_PORT=30110

OAUTH_ADMIN_HOST = localhost
OAUTH_ADMIN_PORT = 4445

OAUTH_PUBLIC_HOST = localhost
OAUTH_PUBLIC_PORT = 4444

AUTHENTICATION_PUBLIC_HOST = localhost
AUTHENTICATION_PUBLIC_PORT = 30110


# Redis - use local instance
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_CLUSTER_MODE=standalone
REDIS_MASTER_GROUPNAME=
REDIS_PASSWORD=eisoo.com123

# Mongo
MONGODB_USER=admin
MONGODB_PASSWORD=111111
MONGODB_HOST=localhost
MONGODB_PORT=28000
MONGODB_NAME=automation
MONGODB_DSNDB=admin
MONGODB_REPLICASET=
MONGODB_DIRECT=false
MONGODB_POOL_MAX=5
MONGODB_POOL_MIN=1
MONGODB_SSL=false

# Database - CRITICAL: Change from Kubernetes DNS to localhost
DB_HOST=localhost
DB_PORT=3330
DB_TYPE=mysql
DB_NAME=adp
DB_USER=anyshare
DB_PASSWORD=eisoo.com123

# You can also override other services as needed:
# OAUTH_ADMIN_HOST=localhost
# OAUTH_PUBLIC_HOST=localhost
# EACP_HOST=localhost
# etc.

# Business Domain Service Mock
BUSINESS_DOMAIN_HOST=localhost
BUSINESS_DOMAIN_PORT=8085

# Kafka/MQ - local instance without authentication
# Use port 9097 which is the host-exposed port (9092 is internal to container)
# Local Kafka uses PLAINTEXT protocol without SASL authentication
MQ_HOST=localhost
MQ_PORT=9097
MQ_CONNECTORTYPE=kafka
MQ_AUTH_USERNAME=
MQ_AUTH_PASSWORD=
MQ_AUTH_MECHANISM=
MQ_PROTOCOL=plaintext

# OSS Gateway Service Mock
OSSGATEWAY_PRIVATE_HOST=localhost
OSSGATEWAY_PRIVATE_PORT=9002

# S3 Configuration (optional - only needed if using S3 data sources)
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=6BIHJAFUM22VFHC9BVJ9
S3_SECRET_ACCESS_KEY=FFz4ztEmyxMicrk+kDFCOUYrtDmfXtbmzG27FVaV
S3_USE_SSL=false
S3_BUCKET_NAME=yannan

SERVER_DAG_INSTANCE_EVENT_ARCHIVE_POLICY=never
