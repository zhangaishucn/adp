{
    "definitions": {
        "Settings": {
            "type": "object",
            "properties": {
                "retry": {
                    "$ref": "#/definitions/Retry"
                },
                "timeout": {
                    "$ref": "#/definitions/TimeOut"
                }
            },
            "required": ["retry", "timeout"],
            "additionalProperties": false
        },

        "Retry": {
            "type": "object",
            "properties": {
                "max": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 10
                },
                "delay": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 60
                }
            },
            "required": [
                "max",
                "delay"
            ],
            "additionalProperties": false
        },

        "TimeOut": {
            "type": "object",
            "properties": {
                "delay": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 86400
                }
            },
            "required": [
                "delay"
            ],
            "additionalProperties": false
        }
    }
}