{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "通过消息删除安全策略流程",
    "description": "default.as.automation.security_policy_flow_delete",
    "type": "object",
    "properties": {
        "id": {
            "description": "流程实例ID",
            "type": "string"
        },
        "status": {
            "description": "运行状态",
            "type": "string",
            "enum": ["canceled"]
        }
    },
    "required": ["id", "status"]
}
