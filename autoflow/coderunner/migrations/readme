migrations目录
    a. init.sql文件
        i. 保存最新版本的数据库初始化语句，用于初次部署时，对数据库表进行初始化；用于执行下一个版本的升级脚本前，检查客户环境表结构是否与升级脚本所基于的表结构一致
        ii. 必需幂等，初始化过程中如果脚本执行失败，会重复执行
        iii. 如果一个服务使用了多个数据库，在init.sql中需在 create table 等操作之前通过 use xxx 指定对应的数据库
    b. 版本号目录，对应服务版本，版本号支持3位及以上，如: 7.0.4.9
        i. pre目录，存放服务升级前执行的脚本
            1. 文件命名规则：“2位数字-”+“描述信息”+“.文件类型”，其中数字表示执行顺序，升级文件支持py类型和sql类型，如：01-xxx.py、02-xxx.sql
            2. 必需幂等，升级过程中如果脚本执行失败，会重复执行
        ii. post目录，存放服务升级后执行的脚本，当前仅允许含一个升级脚本，文件命名规则同上，如：01-xx.sql、01-xx.py
        iii. 升级模型文件如何编写，请参照Feature-361823逻辑设计中的编写示例

参考链接：
https://confluence.aishu.cn/pages/viewpage.action?pageId=183165748
https://confluence.aishu.cn/pages/viewpage.action?pageId=185580092
